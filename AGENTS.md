# AI ワークフロールール — compphys_Shinaoka

計算物理学のためのJulia入門（品岡先生）における AI エージェント利用ガイドライン。

## 前提

このリポジトリは講義の演習コードを管理する。
AI が生成したコードは **必ず人間がレビューしてから使用する**。

---

## 良い AI 開発の原則

### 1. 明確なコンテキストを与える
- 使用パッケージのバージョン制約（`Project.toml` の `[compat]` セクション）を参照させる。
- 解くべき物理的問題・数値手法の前提を最初のプロンプトに含める。
- 曖昧な spell のまま AI に投げない。「モンテカルロ法で何を計算したいか」まで明示する。

### 2. 出力の検証を必ず行う
- AI が生成したコードには `BenchmarkTools` で性能を測定し、期待値と照合する。
- 数値結果は解析解または既知の極限（例: 高温展開・低温展開）と比較して正しさを確認する。
- `Test` モジュールでユニットテストを書き、回帰を防ぐ。

### 3. ファイル変更の範囲を限定する
- 1 回のプロンプトで変更するファイルは 1–2 個に絞る。
- `src/` と `notebook/` を同時に大幅変更するプロンプトは避ける。

### 4. 危険なパターンを見つける（コードレビュー必須箇所）
AI が吐き出したコードで特に確認すべき点:

| カテゴリ | チェック項目 |
|---|---|
| 型安定性 | `@code_warntype` で型推論が壊れていないか |
| メモリ確保 | ループ内で不要な配列確保をしていないか |
| 数値誤差 | 桁落ちを起こす引き算・大きな数の差分がないか |
| ライセンス | コピーしたコードのライセンスが互換かどうか |

---

## 悪い AI 開発のアンチパターン（禁止事項）

- **一方向プロンプト**: 前提を伝えず結果だけ要求し、検証なしにそのまま使う。
- **レビュー省略**: 「動いたから OK」で人間のコードレビューをスキップする。
- **過度な自動化**: AI に `Manifest.toml` の自動更新や `git push` を直接させる。

---

## ファイル構成ルール

```
compphys_Shinaoka/
├── Project.toml      # 依存関係（コミット対象）
├── Manifest.toml     # ロックファイル（.gitignore 対象）
├── src/              # Julia ソースコード
└── notebook/         # Jupyter ノートブック（演習用）
```

- `Manifest.toml` はバージョン管理しない（`.gitignore` 済み）。
- ノートブックのセル出力は原則クリアしてからコミットする。

---

## コミットメッセージ規約

```
<type>: <要約（日本語可）>
```

| type | 用途 |
|---|---|
| `feat` | 新しい演習・機能の追加 |
| `fix` | バグ修正・計算ミスの修正 |
| `perf` | BenchmarkTools で確認した高速化 |
| `test` | テストの追加・修正 |
| `docs` | README・コメントのみの変更 |
